import { useState, useEffect } from "react";
import { usePublicContextForm } from "./useOwnContext";
import { STATUS } from "./../utils/constants";
import { noop } from "./../utils/noop";
export function useChildren(initialState, onReset) {
  if (initialState === void 0) {
    initialState = [];
  }

  if (onReset === void 0) {
    onReset = noop;
  }

  var _useState = useState(function () {
    return initialState;
  }),
      children = _useState[0],
      setChilren = _useState[1];

  var _usePublicContextForm = usePublicContextForm(),
      formStatus = _usePublicContextForm.formStatus;

  useEffect(function () {
    if (formStatus === STATUS.ON_RESET) {
      setChilren([]);
      onReset();
    }
  }, [formStatus]);
  return [children, setChilren];
}